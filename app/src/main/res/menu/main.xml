<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item
        android:id="@+id/menu_search"
        android:title="@string/search_menu"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="always"
        tools:icon="@android:drawable/ic_menu_search"/>

    <item
        android:id="@+id/menu_my_favorited"
        android:title="@string/my_favorited_menu"/>

    <item
        android:id="@+id/menu_recently_played"
        android:title="@string/recently_played_menu"/>

    <item
        android:id="@+id/menu_local_song"
        android:title="@string/local_song_menu"/>
</menu>